% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dh_normalize_counts.R,
%   R/edger_normalize_counts.R
\name{normalizeReadCounts}
\alias{normalizeReadCounts}
\title{Normalize Read Counts using edgeR}
\usage{
normalizeReadCounts(control, treatment)

normalizeReadCounts(control, treatment)
}
\arguments{
\item{control}{A matrix of read counts for the control group.}

\item{treatment}{A matrix of read counts for the treatment group.}
}
\value{
A list containing two matrices with TMM-normalized read counts for
        control and treatment groups.

A list containing two matrices with TMM-normalized read counts for
        control and treatment groups.
}
\description{
This function takes read count matrices for control and treatment groups,
performs normalization using the edgeR package, and returns the TMM-normalized
counts for further analysis.

This function takes read count matrices for control and treatment groups,
performs normalization using the edgeR package, and returns the TMM-normalized
counts for further analysis.
}
\examples{
control <- matrix(rpois(200, lambda = 10), ncol = 5)
treatment <- matrix(rpois(200, lambda = 20), ncol = 5)
result <- normalizeReadCounts(control, treatment)
control_normalized <- result$control
treatment_normalized <- result$treatment
control <- matrix(rpois(200, lambda = 10), ncol = 5)
treatment <- matrix(rpois(200, lambda = 20), ncol = 5)
result <- normalizeReadCounts(control, treatment)
control_normalized <- result$control
treatment_normalized <- result$treatment
}
