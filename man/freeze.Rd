% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freeze.R
\name{freeze}
\alias{freeze}
\title{Convert a Melted Data Frame into a Matrix}
\usage{
freeze(melted_df, value_col = "value", loc1_col = "Var1", loc2_col = "Var2")
}
\arguments{
\item{melted_df}{A data frame in a melted format, typically with columns representing values and their associated locations.}

\item{value_col}{A character string specifying the column name that contains the values to populate the matrix. Default is `"value"`.}

\item{loc1_col}{A character string specifying the column name for the row locations of the matrix. Default is `"Var1"`.}

\item{loc2_col}{A character string specifying the column name for the column locations of the matrix. Default is `"Var2"`.}
}
\value{
A matrix where rows are indexed by the values in `loc1_col`, columns are indexed by the values in `loc2_col`, and the matrix elements are populated by the values in `value_col`.
}
\description{
This function converts a melted data frame into a matrix, where the rows and columns correspond to specified location columns.
}
\details{
The function takes a melted data frame and casts it into a wide format using the specified location columns. The resulting data frame is then converted into a matrix, with row names set by the values in `loc1_col`.
}
\examples{
\dontrun{
melted_df <- data.frame(Var1 = c("A", "A", "B", "B"), Var2 = c("X", "Y", "X", "Y"), value = 1:4)
matrix_result <- freeze(melted_df, value_col = "value", loc1_col = "Var1", loc2_col = "Var2")
}

}
